class Solution {
    public boolean isPalindrome(int x) {
        int a=x;   
        int ans=0;
        while(x>0){
            int rem=x%10;
            ans=ans*10+rem;
            x/=10;
        }
        if(ans==a) return true;
        else {return false;}
        
    }
}
